{% extends 'GalleryMainBundle::layout.html.twig' %}

{% block content %}
    <form action="{{ path('image_delete', { 'id': entity.id } ) }}" method="POST">
        Вы уверенны?
    
        <div class="bottom-buttons">
            <input type="submit" class="btn btn-default btn-sm margin_button" value="Удалить" />
            <button class="btn btn-default btn-sm margin_button" type="button" onclick="history.back()">Отмена</button>
        </div>
        {{ form_rest(form) }}
    </form>
{% endblock %}
