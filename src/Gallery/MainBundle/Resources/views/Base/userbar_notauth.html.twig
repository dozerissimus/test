      <form id="loginform" class="form-inline" action="{{ path("fos_user_security_check") }}" method="post">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
            Вы вошли как
            <font color="blue">Гость</font>
        
            <div class="button_place">
                <div class="form-group">
                    <label class="sr-only" for="InputLogin">Login</label>
                    <input id="login" class="form-control input-sm" type="text" required="required" placeholder="Логин" name="_username">
                </div>
                <div class="form-group">
                    <label class="sr-only" for="InputPassword">Password</label>
                    <input id="pwd" class="form-control input-sm" type="password" required="required" placeholder="Пароль" name="_password">
                </div>
                <button class="btn btn-default btn-sm" type="submit">Войти</button>
                <button class="btn btn-default btn-sm" onclick="location.href='{{ path('fos_user_registration_register') }}'" type="button">Регистрация</button>
            </div>
        </form>